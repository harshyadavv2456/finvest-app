fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
pandas==2.2.3
pyarrow>=18.0.0
numpy==2.1.1
groq>=0.12.0
python-dotenv==1.0.1
requests>=2.31.0
beautifulsoup4>=4.12.0
httpx>=0.27.0
yfinance>=0.2.36
boto3>=1.34.0
supabase>=2.7.0
redis>=5.0.0

# Used by the top-level data pipeline scripts (FinSight/stock_crawler.py,
# FinSight/Smart Money Flow/fii_dii_pipeline.py), which the daily-refresh
# workflow installs this same file for. Missing here broke both jobs
# (ModuleNotFoundError / "nsepython not installed") - 2026-08-21.
feedparser>=6.0.0
nsepython>=2.0.0

# AngelOne SmartAPI (Workstream D) - read-only market data, primary IN
# equity/option source. smartapi-python pulls in logzero transitively but
# doesn't declare it - pinning it explicitly (confirmed missing during
# this session's own install).
smartapi-python>=1.5.5
pyotp>=2.9.0
logzero>=1.7.0
websocket-client>=1.6.0
